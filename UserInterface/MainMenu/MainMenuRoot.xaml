<ek:UIRoot   
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:ek="clr-namespace:EmptyKeys.UserInterface.Designer;assembly=EmptyKeys.UserInterface.Designer"
             xmlns:local="clr-namespace:UserInterface"
             mc:Ignorable="d" 
             Background="Transparent"
             d:DesignHeight="1080" d:DesignWidth="1920">

    <Grid Background="Transparent">

        <local:MainMenuFirstUserControl
            Visibility="{Binding FirstMenuVisible, Mode=TwoWay}"/>
        <local:MainMenuConnectUserControl
            Visibility="{Binding ConnectMenuVisible, Mode=TwoWay}"/>
        <local:MainMenuJoinRoomUserControl
            Visibility="{Binding JoinRoomMenuVisible, Mode=TwoWay}"/>
        <local:MainMenuRegisterUserControl
            Visibility="{Binding RegisterMenuVisible, Mode=TwoWay}"/>
        <local:MainMenuSettingsUserControl
            Visibility="{Binding SettingsMenuVisible, Mode=TwoWay}"/>
    </Grid>
</ek:UIRoot>